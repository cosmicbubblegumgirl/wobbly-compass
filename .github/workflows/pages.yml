name: Publish site

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v6

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Prepare public files
        env:
          FORM_API_BASE: ${{ secrets.FORM_API_BASE }}
        run: |
          test -n "$FORM_API_BASE"
          mkdir _site
          cp index.html styles.css app.js .nojekyll _site/
          node -e "const fs=require('fs');const p='_site/app.js';const s=fs.readFileSync(p,'utf8').replace('__FORM_API_BASE__',process.env.FORM_API_BASE);fs.writeFileSync(p,s)"

      - name: Upload site
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
